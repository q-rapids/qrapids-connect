name=kafka-taiga-elasticsearch
connector.class=io.confluent.connect.elasticsearch.ElasticsearchSinkConnector
tasks.max=1

# name of elasticsearch index
topics =

# ignore=FALSE using gitlab id as index document id (_id)
# ignore=TRUE index document id (_id) is autogenerated
key.ignore=true

# elasticsearch url
connection.url=http://<elasticsearch>:9200

# elasticsearch index mapping name
type.name=taiga